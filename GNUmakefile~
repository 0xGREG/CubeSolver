make:;
	g++ solver.cpp classes/* globals/* -o solver
install:;
	@echo "Installing solver..."
	@cp solver /usr/bin/
clean:;
	@echo "Removing compiled binaries"
	@rm solver
uninstall:;
	@echo "Uninstalling solver from system"
	@rm /usr/bin/solver
